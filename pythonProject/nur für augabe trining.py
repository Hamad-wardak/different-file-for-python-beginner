'''Here is different function '''
x = 22.4545499
y = 3
w = '34'
print(x ** y) # thie fuction can you for power use:
print(round(x)) # That is round off function in math.
Ahmad = "ich kann moregen nicht kommen, weil ich die hausaufgabe nicht gemacht. können sie um 17:00 uhr zu Meine Hause kommen Danke schön::"
print(Ahmad.upper()) # we can use the upper function to make all the krecktors larger.
print(Ahmad.lower()) #we can use upper function to make all krecktors smaller
print(Ahmad[3:33]) # the function means: press me in ahmad text variable 3 to 33 kraktors.
print(Ahmad[8:90]) # The function means: press me in ahmad text variable 8 to 38 kraktors.
# this sub function is finished
print("ein aufgabe ist fertig")
colors = ['red', 'yelow', 'green', 'blue'] # this is a string variable in list.
numbers = [3, 4, 5, 90, 90, 3, 4, 88, 55, 67] #this is an integer variable.
print(len(numbers))
print(set(numbers)) # With set functions we can put things into a variable without pressing twice. Everything is similar to pressing once.
print(len(colors)) # count me how much in colors variable which is strict or integer.
print(numbers[4]) # please express me in numbers variable fourth integer.
print(numbers[-2]) # press me in numbers variable second integer but to the left.
colors.append('pink')# in a variable we can make a strict input with append but in end the variable.
print(colors)
colors.insert(2,'orange') # in a variable we can make a strict input with insert function but in how I want in the variable
#index is the x position in function and object is the x(items)
print(colors)
print(numbers.sort()) # we can use this sort function to make the numbers ascending in a variable.
print(numbers)

print(numbers.reverse()) # With this reverse function we can make the numbers in descending order in a variable. print(numbers)
print("wow")
for index, colors in enumerate(colors): # I can put this function in variable which strictly with numbrers vertical list.
    print(index, colors,)
for numbers in numbers: # With this FOR and IN function you can put the strict or integer in vertical list.
    print(numbers)
for colors in colors:
    print(colors) #With this FOR and IN function you can put the strict or integer in vertical list.
